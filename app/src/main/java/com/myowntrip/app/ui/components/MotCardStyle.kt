package com.myowntrip.app.ui.components

/**
 * Superficie de card — alineado a Figma `Stacked card` / `Horizontal card` (eje Style).
 * Filled está CUT en el DS; no exponer.
 */
enum class MotCardStyle {
  Outlined,
  Elevated,
}
